# Configuration for running connectors in bundle with connector-runtime

# the clientSecret must be updated when the server run in the authentication m
#     Environment:
camunda:
  client:
    mode: oidc
    auth:
      clientId: zeebe
      clientSecret: kw9k80pTDg
    zeebe:
      enabled: true
      preferRestOverGrpc: false
      audience: zeebe-api
      gateway-url: http://localhost:26500
      security:
        plaintext: false

  # Don't run the connection to Operate (not an inbound connection)
  connector:
    polling:
      enabled: false
    webhook:
      enabled: false



spring.main.web-application-type: none
operate.client.enabled: true
